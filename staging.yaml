service: uapply-staging-api
runtime: custom # Updated runtime
env: flex
manual_scaling:
  instances: 1
env_variables:
  APP_ENV: 'staging'
network:
  session_affinity: true # Helps maintain WebSocket connections
resources:
  cpu: 1
  memory_gb: 2.3
  disk_size_gb: 10
